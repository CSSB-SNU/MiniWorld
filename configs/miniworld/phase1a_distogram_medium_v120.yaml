# v1.2.0 Phase 1a medium: v1.2.0 data policy with the PF16 trunk of the v1.1 medium run.
# Four GPUs x one sample x accumulation 64 preserves effective batch 256.
defaults:
  - phase1a_distogram_v120
  - _self_

model:
  trunk:
    pairformer:
      n_block: 16

train:
  comment: "v1.2.0-phase1a-distogram-medium-L384"
  run_dir: "runs/v1.2.0/phase1a/medium_distogram_L384"
  grad_accum_steps: 64
  engine_backend: auto
