logs/
runs/
checkpoints/
*.pt
*.cif
*.mmcif
output/
outputs/
*.sh
wandb/
submits/
*.pyc
__pycache__/
*.py[cod]
*$py.class
*.pkl
.pixi/
**/*.egg-info/
commands/
tests/inference_data/
.codex.cache/
/inference/
# one-off experiment/bench/probe scripts, not for main
scratch/
# local dependency checkouts (separate git repos, NOT submodules; foldbench is ~147G)
/libs/datacooker/
/libs/foldbench/
/libs/kmer_fast_align/
# meaningful-but-untracked local artifacts (figures, bioai logs, tools, adhoc tests)
/temp/
# downloaded reference PDFs for the tech report (not for git)
docs/refs/
# LaTeX build artifacts for the tech report (source + refs.bib are tracked)
docs/paper/*.aux
docs/paper/*.bbl
docs/paper/*.blg
docs/paper/*.log
docs/paper/*.out
docs/paper/*.synctex.gz
docs/paper/main.pdf
# Local FoldBench runs and per-target outputs; publish source and reports separately.
/eval_results/

# Separate engine worktrees and local proof environment.
/.engine-release-2.0.0/
/.engine-v2-proof/
