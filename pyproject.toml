[project]
authors = [{ name = "Sanggeun Park", email = "psk6950@snu.ac.kr" }]
name = "miniworld"
requires-python = ">= 3.10"
version = "1.0.0"
dependencies = [
  "torch>=2.8.0,<3",
  "wandb>=0.19.8",
  "omegaconf>=2.3.0,<3",
  "numpy>=2.2.5,<3",
  "einops>=0.8.1,<0.9",
  "jaxtyping>=0.3.2,<0.4",
  "beartype>=0.21.0,<0.22",
  "scipy>=1.15.3,<2",
  "lightning>=2.5.6,<3",
  "biomol>=1.0.0",
  "cuequivariance-torch>=0.9.0,<0.10",
  "pyyaml>=6,<7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.setuptools]
include-package-data = true


[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.ruff.lint]
select = ["ALL"]
ignore = [
  "E731",
  "E741",
  "F722",    # Incompatible with jaxtyping
  "S311",
  "D105",
  "D100",
  "D107",
  "D203",
  "D213",
  "PLR0913",
  "ANN401",
  "N812",
  "E501",
  "N806",
  "FBT001",
  "FBT002",
  "PLR2004",
  "NPY002",
  "TC001",
  "TC002",
]


[tool.pixi.feature.cuda13.system-requirements]
cuda = "13"

[tool.pixi.feature.cuda13.pypi-dependencies]
cuequivariance-ops-torch-cu13 = "*"
# Optional: FlashAttention-4 (Hopper/Blackwell, sm_100/B200) for the `flash`
# backend of the opt-in SWA atom attention (configs/.../*_swa.yaml). FA4 is a
# CuTeDSL pure-Python wheel that JIT-compiles CUTLASS kernels, so it is not
# pinned to the torch ABI. It is a prerelease and not enabled by default to
# avoid forcing it on the training env; install it explicitly when running the
# SWA experiment:
#   uv pip install --python .pixi/envs/default/bin/python3.10 \
#       --prerelease=allow "flash-attn-4[cu13]"
# The SWA attention falls back to the native FlexAttention backend if absent.

[tool.pixi.feature.cuda13.pypi-options]
extra-index-urls = ["https://download.pytorch.org/whl/cu130"]

[tool.pixi.feature.cuda128.system-requirements]
cuda = "12.8"

[tool.pixi.feature.cuda128.pypi-dependencies]
cuequivariance-ops-torch-cu12 = "*"

[tool.pixi.feature.cuda128.pypi-options]
extra-index-urls = ["https://download.pytorch.org/whl/cu128"]

[tool.pixi.environments]
default = { features = ["cuda13"] }
cu128 = { features = ["cuda128"] }

[tool.pixi.pypi-dependencies]
miniworld = { path = ".", editable = true }
team_gm = { path = "libs/team-gm", editable = true }

[tool.pixi.dependencies]
pytest = ">=8.3.5,<9"
python = "~=3.10.16"
ruff = ">=0.11.6,<0.12"
ipykernel = ">=7.1.0,<8"
matplotlib = ">=3.10.7,<4"
pandas = ">=2.3.3,<3"
tqdm = ">=4.67.1,<5"
click = ">=8.3.1,<9"
biopython = ">=1.87,<2"

[tool.ruff.lint.per-file-ignores]
"scripts/*" = ["D", "ANN201", "S101", "INP001", "FBT001", "PLC0415", "C901"]
"src/miniworld/modules/kernels/*" = ["N806", "N803", "ANN", "ARG001", "D"]
"src/miniworld/utils/diffusion/*" = ["RUF", "N803",]
"src/miniworld/data/pipeline/msa.py" = ["N802","N803"]
"src/miniworld/utils/structure/se3.py" = ["N802","N803", "RET504","E741"]


[tool.ruff]
line-length = 89
target-version = "py310"


[tool.pyright]
enableReachabilityAnalysis = false

[tool.hatch.build.targets.wheel]
packages = [
  "src/miniworld",
]
